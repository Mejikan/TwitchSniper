
import { isNoResults } from 'twitch-sniper/helpers/is-no-results';
import { module, test } from 'qunit';

module('Unit | Helper | is no results');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNoResults([42]);
  assert.ok(result);
});

