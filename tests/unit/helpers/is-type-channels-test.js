
import { isTypeChannels } from 'twitch-sniper/helpers/is-type-channels';
import { module, test } from 'qunit';

module('Unit | Helper | is type channels');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTypeChannels([42]);
  assert.ok(result);
});

