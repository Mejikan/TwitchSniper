
import { isTypeStreams } from 'twitch-sniper/helpers/is-type-streams';
import { module, test } from 'qunit';

module('Unit | Helper | is type streams');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTypeStreams([42]);
  assert.ok(result);
});

