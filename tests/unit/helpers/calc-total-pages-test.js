
import { calcTotalPages } from 'twitch-sniper/helpers/calc-total-pages';
import { module, test } from 'qunit';

module('Unit | Helper | calc total pages');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcTotalPages([42]);
  assert.ok(result);
});

