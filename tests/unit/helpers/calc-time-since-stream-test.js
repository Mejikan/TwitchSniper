
import { calcTimeSinceStream } from 'twitch-sniper/helpers/calc-time-since-stream';
import { module, test } from 'qunit';

module('Unit | Helper | calc time since stream');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcTimeSinceStream([42]);
  assert.ok(result);
});

